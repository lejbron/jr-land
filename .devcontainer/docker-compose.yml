services:
  web:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    user: node
    ports:
      - "4321:4321"
    volumes:
      - ..:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: sleep infinity
